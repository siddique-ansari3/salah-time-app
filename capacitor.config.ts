import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'salah-time-app',
  webDir: 'dist/salah-time-app/browser',
};

export default config;
